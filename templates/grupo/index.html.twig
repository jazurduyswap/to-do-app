{% extends 'base.html.twig' %}

{% block title %}Grupos{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header d-flex justify-content-between align-items-center">
                    <h1 class="mb-0">Grupos</h1>
                    <a href="{{ path('app_homepage') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-house"></i> Inicio
                    </a>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h4>Módulo de Grupos</h4>
                        <p class="mb-0">Aquí podrás gestionar los grupos de la aplicación.</p>
                    </div>
                    
                    <div class="text-center">
                        <p class="text-muted">Funcionalidad en desarrollo...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

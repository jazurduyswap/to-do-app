<a href="/usuarios">Atrás</a>
<br>
<h1>{{ usuario.nombre }}</h1>
<p>Usuario {{ usuario.nombre }} con id {{ usuario.id }} </p>
<br>
<a href="/usuarios/editar/{{ usuario.id }}">Editar</a>
<form method="post" action="{{ path('usuario_borrar', { id: usuario.id }) }}" onsubmit="return confirm('¿Estás seguro de eliminar este usuario?');">
    <button type="submit">Eliminar</button>
</form>